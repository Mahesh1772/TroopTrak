import 'package:flutter/material.dart';

class GuardDutyScreen extends StatelessWidget {
  const GuardDutyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Guard Duty Screen - Coming Soon'),
    );
  }
}